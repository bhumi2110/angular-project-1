import React from 'react';
import { HeaderWrapper,ParaWrapper,OW } from './Header.styled';

const Header = () => (
 <HeaderWrapper>
    this is heading in div tag
    <ParaWrapper>
      this is paragraph
    </ParaWrapper>
    <OW>this is unorsered list</OW>
 </HeaderWrapper>
);
export default Header;
